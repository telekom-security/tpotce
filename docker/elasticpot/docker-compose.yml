networks:
  elasticpot_local:

services:

# Elasticpot service
  elasticpot:
    build: .
    container_name: elasticpot
    restart: always
    networks:
     - elasticpot_local
    ports:
     - "9200:9200"
    image: "dtagdevsec/elasticpot:24.04.1"
    read_only: true
    volumes:
     - $HOME/tpotce/data/elasticpot/log:/opt/elasticpot/log
