networks:
  hellpot_local:

services:

# hellpot service
  hellpot:
    build: .
    container_name: hellpot
    restart: always
#    cpu_count: 1
#    cpus: 0.25
    networks:
     - hellpot_local
    ports:
     - "80:8080"
    image: "ghcr.io/telekom-security/hellpot:24.04.1"
    read_only: true
    volumes:
     - $HOME/tpotce/data/hellpot/log:/var/log/hellpot
